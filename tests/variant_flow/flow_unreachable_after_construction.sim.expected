enum Option: Copy + Drop {
  None: unit
  Some: i64
}

fn f() {
  o: Option;
  entry:
    o = Option::None(unit);
    switchEnum(o) [None: n, Some: dead]
  n:
    drop o;
    return
  dead:
    unreachable
}
