
            enum Option: Copy + Drop { None: unit Some: i64 }
            fn f(b: bool) {
              o: Option;
              entry:
                branch(copy b) [true: t, false: fbr]
              t:
                o = Option::None(unit);
                goto join
              fbr:
                o = Option::Some(42);
                goto join
              join:
                switchEnum(o) [None: n, Some: dead]
              n: return
              dead: unreachable
            }
            