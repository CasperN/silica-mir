enum Option: Copy + Drop {
  None: unit
  Some: i64
}

fn f() {
  o: Option;
  entry:
    o = Option::None(unit);
    switchEnum(o) [None: n, Some: s]
  n:
    drop o;
    return
  s:
    drop o;
    return
}
