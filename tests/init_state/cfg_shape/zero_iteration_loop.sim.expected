extern fn sink(r: &mut i64);

fn f(b: bool, x: i64) {
  r: &mut i64;
  entry:
    r = &mut x;
    goto head
  head:
    branch(copy b) [true: body, false: done]
  body:
    goto head
  done:
    call sink(move r);
    drop x;
    drop b;
    return
}
