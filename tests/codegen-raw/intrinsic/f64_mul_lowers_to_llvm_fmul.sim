
        fn f(a: f64, b: f64) {
          r: f64;
          out: &out f64;
          entry:
            out = &out r;
            call $f64_mul(copy a, copy b, move out);
            return
        }
        