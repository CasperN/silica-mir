extern fn use_num(n: i64);

fn f(x: i64, b: bool) {
  r: &mut i64;
  entry:
    branch(copy b) [true: t, false: fbr]
  t:
    r = &mut x;
    abort
  fbr:
    call use_num(copy x);
    drop b;
    drop x;
    return
}
