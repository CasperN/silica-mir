extern fn sink(r: &mut i64);

fn f(x: i64, b: bool) {
  r: &mut i64;
  entry:
    r = &mut x;
    branch(copy b) [true: t, false: fbr]
  t:
    abort
  fbr:
    call sink(move r);
    drop b;
    drop x;
    return
}
