# Create &drop, transfer via call. Loan obligation delegated to
# the callee.
extern fn consume(r: &drop i64);
fn f(x: i64) {
  r: &drop i64;
  entry:
    r = &drop x;
    call consume(move r);
    return
}
