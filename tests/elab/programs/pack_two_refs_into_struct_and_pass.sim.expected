struct Pair: Move {
  a: &mut i64
  b: &mut i64
}

extern fn use_pair(p: Pair);

fn f(x: i64, y: i64) {
  p: Pair;
  entry:
    p.a = &mut x;
    p.b = &mut y;
    call use_pair(move p);
    drop y;
    drop x;
    return
}
