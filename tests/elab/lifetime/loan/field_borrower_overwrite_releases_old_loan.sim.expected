struct RefBox: Move {
  p: &mut i64
}

extern fn take_box(b: RefBox);

fn f(x: i64, x2: i64) {
  b: RefBox;
  entry:
    b.p = &mut x;
    drop b.p;
    b.p = &mut x2;
    drop x;
    x = 42;
    call take_box(move b);
    drop x2;
    drop x;
    return
}
