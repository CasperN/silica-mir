struct Inner: Copy + Drop {
  x: i64
  y: i64
}

struct Outer: Copy + Drop {
  i: Inner
  c: i64
}

extern fn use_mut(r: &mut i64);

fn f(o: Outer) {
  r: &mut i64;
  z: i64;
  entry:
    r = &mut o.i.x;
    z = copy o.i.y;
    call use_mut(move r);
    drop z;
    drop o;
    return
}
