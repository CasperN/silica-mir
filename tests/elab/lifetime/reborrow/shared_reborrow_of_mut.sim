
        extern fn read_ref(r: &i64);
        fn f(x: i64) {
          r: &mut i64;
          s: &i64;
          entry:
            r = &mut x;
            s = &r.*;
            call read_ref(move s);
            return
        }
        