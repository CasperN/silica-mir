struct Voidish: Copy + Drop {
  real: i64
  fake: never
}

enum E: Copy + Drop {
  A: i64
  N: Voidish
}

fn f(e: E, out: &out i64) {
  entry:
    switchEnum(e) [A: a_arm, N: n_arm]
  a_arm:
    out.* = copy e as A;
    unborrow out;
    drop e;
    return
  n_arm:
    unreachable
}
