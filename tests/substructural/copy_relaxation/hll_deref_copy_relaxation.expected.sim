struct Pair: Copy + Drop {
  left: i64
  right: i64
}

fn<'s0, 's1> read_shared_field(r: & 's0 Pair, $return: &out 's1 i64) {
  entry:
    $return.* = copy r.*.left;
    unborrow $return;
    unborrow r;
    require_uninit $return;
    require_uninit r;
    return
}
