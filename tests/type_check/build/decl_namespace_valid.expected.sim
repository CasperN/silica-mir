fn mixed_distinct_names() {
  entry:
    return
}

struct Point {
  x: i64
  y: i64
}

enum Color: Copy + Drop {
  Red: $Tuple0
  Green: $Tuple0
  Blue: $Tuple0
}

extern fn sink(n: i64);

struct T {
  v: i64
}

fn T() {
  entry:
    return
}
