# Impl methods must have bodies — a body-less form is only valid on
# trait method signatures. Parse errors short-circuit; see sibling
# fixtures for the other impl-method parse rejections.

struct S { x: i64 }

trait T {
  fn m(recv: & Self);
}

impl T for S {
  fn m(recv: & S);
}
