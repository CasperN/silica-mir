# Impl methods cannot be `extern` — impl bodies are Silica-ABI.
# Parse errors short-circuit; see sibling fixtures for the other
# impl-method parse rejections.

struct S { x: i64 }

trait T {
  fn m(recv: & Self);
}

impl T for S {
  extern fn m(recv: & S);
}
