struct RefBox: Move {
  p: &mut i64
}

extern fn sink(r: &mut i64);

extern fn take_box(b: RefBox);

fn f(x: i64) {
  b: RefBox;
  s: &mut i64;
  entry:
    b.p = &mut x;
    s = &mut b.p.*;
    call sink(move s);
    call take_box(move b);
    drop x;
    return
}
