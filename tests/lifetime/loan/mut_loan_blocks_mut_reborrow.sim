
        extern fn sink(r: &mut i64);
        fn f(x: i64) {
          r: &mut i64;
          s: &mut i64;
          entry:
            r = &mut x;
            s = &mut x;
            call sink(move r);
            return
        }
        