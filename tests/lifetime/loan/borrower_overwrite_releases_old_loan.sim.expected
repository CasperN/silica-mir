extern fn use_mut(r: &mut i64);

fn f(x: i64, y: i64) {
  r: &mut i64;
  entry:
    r = &mut x;
    unborrow r;
    r = &mut y;
    drop x;
    x = 42;
    call use_mut(move r);
    drop y;
    drop x;
    return
}
