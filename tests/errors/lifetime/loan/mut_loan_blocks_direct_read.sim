
        extern fn sink(r: &mut i64);
        fn f(x: i64) {
          r: &mut i64;
          y: i64;
          entry:
            r = &mut x;
            y = copy x;
            call sink(move r);
            return
        }
        