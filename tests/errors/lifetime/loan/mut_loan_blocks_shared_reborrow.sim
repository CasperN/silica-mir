
        extern fn sink(r: &mut i64);
        fn f(x: i64) {
          r: &mut i64;
          s: &i64;
          entry:
            r = &mut x;
            s = &x;
            call sink(move r);
            return
        }
        