
        extern fn use_mut(r: &mut i64);
        fn f(i: i64) {
          a: [i64; 3];
          r: &mut i64;
          entry:
            r = &mut a[copy i];
            call use_mut(move r);
            return
        }
        